class ResolveService {
    login($q) {
        'ngInject';
        return error;

        function error() {
            return $q.reject('requireLogin');
        }
    }
}

export default ResolveService;
